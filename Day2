print("Welcome to the tip calculator!")
total_bill=float(input("what was the total bill ? "))
tip_bill=float(input("How much would you like to give? 10, 12, or 15?"))
number_of_people=float(input("How many people to  split the bill ? "))

final_formulata= (total_bill + tip_bill ) / number_of_people
print("Each person should pay:", final_formulata)
